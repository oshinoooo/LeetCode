#include <iostream>
#include <vector>
#include <algorithm>
#include <stack>
#include <numeric>
#include <map>
#include <unordered_map>

using namespace std;

class Solution
{
public:
    bool isMatch(string s, string p)
    {
        for (int i = 0; i < s.size(); ++i)
        {
            
        }
    }
};

int main()
{
    cout << "------------------" << endl;
    Solution s;
    cout << s.isMatch("tree", "sadasd") << endl;
    cout << "------------------" << endl;
    return 0;
}