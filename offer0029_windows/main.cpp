#include <iostream>
#include <vector>

using namespace std;

class Solution
{
public:
    vector<int> spiralOrder(vector<vector<int>>& matrix)
    {
        vector<int> out;
        while (1)
        {
            out.
        }

        return out;
    }
};

int main()
{
    cout << "------------------" << endl;
    vector<vector<int>> matrix = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
    Solution s;
    for (auto out : s.spiralOrder(matrix))
        cout << out << " " << endl;
    cout << "------------------" << endl;
    return 0;
}
